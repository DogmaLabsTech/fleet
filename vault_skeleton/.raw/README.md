# .raw — source inbox

Drop source documents here — articles, transcripts, notes, screenshots — then ingest
them into `wiki/`. Treat files here as immutable: read them to build pages, but edit
the pages, not the sources.

If you install the claude-obsidian engine, its `/ingest` command reads from this
folder.
