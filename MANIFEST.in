# Ship data dirs that live under the engine package inside the wheel and sdist.
# `graft` is dotfile- and binary-inclusive; glob package-data silently drops both.
graft engine/vault_skeleton
graft engine/ui
